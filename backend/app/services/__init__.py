"""Application services: orchestration between routers, the database and
the pure domain modules. Services assemble inputs and persist outputs;
they never compute money figures themselves (the domain engine does).
"""
