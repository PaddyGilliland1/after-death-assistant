"""API routers, one module per area (contract section 8).

Routers currently wired in app.main: health, me. Further routers (assets,
liabilities, contacts, tasks, costs, iht, knowledge, agents, ...) are added
here as their modules land.
"""
